<li class="comment">
  <p>
    <strong><%= time_ago_in_words(comment.created_at) %> ago</strong>
    <%= link_to(
      'Edit',
      edit_polymorphic_path([@project, @commentable, comment]),
      :class => 'text-muted'
    ) %>
  </p>
  <%= comment.comment %>
  <br>
</li>
